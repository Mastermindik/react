import { Dropdown, DropdownButton, FormControl, InputGroup } from "react-bootstrap"

function Categories() {
  return <FormControl as='select' className="select">
    <option>Not Selected</option>   
  </FormControl>
}

export default Categories